name: Weekly Exhibition Content Generator

on:
  schedule:
    # 매주 월요일 오전 9시 (KST) = 일요일 자정 (UTC)
    - cron: '0 0 * * 1'
  workflow_dispatch: # 수동 실행 가능

env:
  ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

jobs:
  generate-exhibition-content:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Fetch exhibition data and generate content
        run: node scripts/generate-exhibition-content.js

      - name: Check for changes
        id: check_changes
        run: |
          if [[ -n $(git status --porcelain) ]]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi

      - name: Create Pull Request
        if: steps.check_changes.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "content: add weekly exhibition content"
          title: "[Auto] Weekly Exhibition Content Update"
          body: |
            ## 자동 생성된 박람회 콘텐츠

            이번 주 베트남 박람회 일정 기반으로 생성된 콘텐츠입니다.

            ### 포함 내용
            - 박람회 일정 업데이트
            - 관련 도메인 용어 큐레이션
            - SEO 랜딩 페이지 (해당 시)

            ---
            🤖 Generated by GitHub Actions
          branch: auto/exhibition-content
          delete-branch: true
          labels: |
            auto-generated
            content
